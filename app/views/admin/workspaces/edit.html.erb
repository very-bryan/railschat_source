<% content_for(:page_title, "워크스페이스 수정") %>

<div class="max-w-4xl mx-auto">
  <div class="mb-8">
    <%= link_to admin_workspace_path(@workspace), class: "inline-flex items-center text-sm text-slate-600 hover:text-slate-900" do %>
      <%= icon "arrow-left", class: "w-4 h-4 mr-1" %>
      워크스페이스 상세로 돌아가기
    <% end %>
  </div>

  <div class="bg-white rounded-lg shadow-sm border border-slate-200 p-6">
    <h1 class="text-2xl font-bold text-slate-900 mb-6">워크스페이스 정보 수정</h1>
    
    <%= form_with model: @workspace, url: admin_workspace_path(@workspace), local: true do |form| %>
      <% if @workspace.errors.any? %>
        <div class="rounded-md bg-red-50 p-4 mb-6">
          <div class="flex">
            <div class="flex-shrink-0">
              <%= icon "alert-circle", class: "h-5 w-5 text-red-400" %>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-800">
                <%= pluralize(@workspace.errors.count, "error") %> prohibited this workspace from being saved:
              </h3>
              <div class="mt-2 text-sm text-red-700">
                <ul class="list-disc list-inside">
                  <% @workspace.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <div class="space-y-6">
        <div>
          <%= form.label :name, "워크스페이스 이름", class: "block text-sm font-medium text-slate-700" %>
          <%= form.text_field :name, class: "mt-1 block w-full rounded-md border-slate-300 shadow-sm focus:border-[#FF6D75] focus:ring-[#FF6D75] sm:text-sm" %>
        </div>

        <div>
          <%= form.label :description, "설명", class: "block text-sm font-medium text-slate-700" %>
          <%= form.text_area :description, rows: 3, class: "mt-1 block w-full rounded-md border-slate-300 shadow-sm focus:border-[#FF6D75] focus:ring-[#FF6D75] sm:text-sm" %>
          <p class="mt-1 text-sm text-slate-500">워크스페이스에 대한 간단한 설명을 입력하세요.</p>
        </div>
      </div>

      <div class="mt-6 flex items-center justify-end gap-3">
        <%= link_to "취소", admin_workspace_path(@workspace), class: "px-4 py-2 border border-slate-300 rounded-md text-sm font-medium text-slate-700 bg-white hover:bg-slate-50" %>
        <%= form.submit "저장", class: "px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-[#FF6D75] hover:bg-[#FF5561] cursor-pointer" %>
      </div>
    <% end %>
  </div>
</div>